export enum DiscussionCategory {
    PROPOSAL = 'proposal',
    PROJECT = 'project',
    INVESTMENT = 'investment',
    COMMUNITY = 'community',
}

export enum MessageType {
    TEXT = 'text',
    FILE = 'file',
    GIF = 'gif',
    IMAGE = 'image',
}
